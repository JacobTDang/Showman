# EE 230 lessons — implementation plan (sub-project 1)

Spec: `docs/superpowers/specs/2026-09-03-ee230-lessons-design.md`. TDD throughout;
`npm run verify` green before each commit.

1. **Kit** — `src/lessons/ee/kit.ts`, `test/unit/ee-kit.test.ts`. scopePane, bodePane,
   equationPane, eeLesson. Physics assertions: −3.01 dB / −45° at the corner; chirp
   sampling bound; narration non-overlap; valid deterministic spec.
2. **Flagship** — `ee.theoremOne` in `src/lessons/ee/theoremOne.ts`. Proves the chain
   end to end: RC circuit, three-decade sweep, scope chirp + Bode dot in lockstep,
   live |T| and ∠T counters.
3. **Tier 1** — `ee.rcFilters`, `ee.transferCharacteristic`, `ee.polesStepResponse`.
4. **Tier 0** — `ee.sinusoids`, `ee.impedancePhasors`, `ee.capacitorInductor`,
   `ee.ohmKvlKcl`.
5. **Catalog + routing** — `src/catalog/ee/lessons.tool.ts` registering all eight as
   scene-level tools; phrases in `schematicRouting.ts`; `catalog.test.ts` sweeps them.
6. **Verify, push.**
