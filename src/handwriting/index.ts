/**
 * Handwriting — line art that draws itself: writeOn (draw-on any polyline/path) and penStroke (a
 * self-writing stroke with a nib that tracks the drawing tip). Pure + deterministic.
 */

export * from "./handwriting.js";
