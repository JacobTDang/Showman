/**
 * Math animation toolkit — primitives are in the engine (arc, counter, polyline);
 * this module adds the math motion presets and composite builders that compose
 * them into kid-friendly math visuals (coordinate planes, graphs, number lines,
 * fractions, …).
 */

export * from "./presets.js";
export * from "./builders.js";
