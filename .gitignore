node_modules/
dist/
coverage/
out/
data/
data-smoke/
control-plane/bin/
*.log
.DS_Store

# Secrets — never commit real keys (OPENROUTER_API_KEY, ANTHROPIC_API_KEY, …)
.env
.env.*
!.env.example

# Local scratch / caches not part of the engine
db_cache/

# Golden test scratch output (regenerated, never committed except the blessed goldens)
test/golden/__received__/

# D1/D2 e2e eval artifacts (regenerated every run; uploaded as CI artifacts, not committed)
orchestrator/eval-scorecards.json
orchestrator/eval-live-scorecards.json
